node_modules/
dist/

# Build artifacts — these get attached to GitHub releases, not committed.
*.ipa
*.apk
__pycache__/
*.pyc
yt_to_mp3.py

# Live YouTube session cookies (exported via a browser extension) used by
# the Import feature's downloader engine -- never commit real auth cookies.
downloader/cookies.txt

# Secrets — the Android release signing keystore must never be public.
android/keystore/

# Portable JDKs used locally to work around a broken system Java on this
# machine (Selector.open() can't establish its loopback pipe on Java 17+).
android/jdk-17.0.20+8/
android/jdk-11.0.32+9/

# IDE
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
